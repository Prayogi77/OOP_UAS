<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.crud">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".KonsumenActivity"></activity>
        <activity android:name=".KonsumenAdapter" />
        <activity android:name=".EditeKonsumenActivity" />
        <activity android:name=".ProdusenAdapter" />
        <activity android:name=".Database.ProdusenActivity" />
        <activity android:name=".ProdusenActivity" />
        <activity android:name=".EditeProdusenActivity" />
        <activity android:name=".EditKonsumenActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>